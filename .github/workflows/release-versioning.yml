# This workflow runs on pushes to release/hotfix branches for CI validation and semantic-release
name: Release Versioning

on:
  push:
    branches:
      - 'release/v**'
      - 'hotfix/v**'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Dummy lint step 1
        run: echo "Running lint check - all files pass linting!"
      - name: Dummy lint step 2
        run: echo "TypeScript compilation successful - no errors found!"
      - name: Dummy lint step 3
        run: echo "Code style validation complete - formatting looks good!"

  unit-test:
    name: Unit Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Dummy unit test step 1
        run: echo "Running unit tests - 25 tests passed!"
      - name: Dummy unit test step 2
        run: echo "Code coverage 95% - exceeds minimum threshold!"
      - name: Dummy unit test step 3
        run: echo "All test suites completed successfully!"

  ui-test:
    name: UI Test
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - name: Dummy UI test step 1
        run: echo "Starting browser automation tests..."
      - name: Dummy UI test step 2
        run: echo "Running Playwright tests - 15 scenarios passed!"
      - name: Dummy UI test step 3
        run: echo "UI test suite completed - all user flows working!"
      - name: Dummy UI test step 4
        run: echo "Generated test report and screenshots"

  version-and-tag:
    name: Version and Tag
    runs-on: ubuntu-latest
    needs:
      - lint
      - unit-test
      - ui-test
    outputs:
      version: ${{ steps.semrel.outputs.version || steps.hotfix_version.outputs.version }}
      tag_name: ${{ steps.create_tag.outputs.tag || steps.hotfix_tag.outputs.tag }}
      is_hotfix: ${{ steps.check_branch.outputs.is_hotfix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: false

      - name: Check branch type
        id: check_branch
        run: |
          BRANCH_NAME="${{ github.ref_name }}"
          if [[ "$BRANCH_NAME" == hotfix/* ]]; then
            echo "is_hotfix=true" >> $GITHUB_OUTPUT
            echo "🔥 Detected hotfix branch: $BRANCH_NAME"
          else
            echo "is_hotfix=false" >> $GITHUB_OUTPUT
            echo "🚀 Detected release branch: $BRANCH_NAME"
          fi

      - name: Setup Node.js
        if: steps.check_branch.outputs.is_hotfix == 'false'
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: Install dependencies
        if: steps.check_branch.outputs.is_hotfix == 'false'
        run: npm ci

      - name: Configure Git
        if: steps.check_branch.outputs.is_hotfix == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Run semantic-release
        id: semrel
        if: steps.check_branch.outputs.is_hotfix == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_PAT }}
        run: |
          echo "🏷️ Running semantic-release (version, changelog, and tagging)"
          npx semantic-release --debug

      - name: Create tag output for release
        id: create_tag
        if: steps.check_branch.outputs.is_hotfix == 'false' && steps.semrel.outputs.version
        run: |
          TAG_NAME="v${{ steps.semrel.outputs.version }}"
          echo "tag=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "✅ Version: ${{ steps.semrel.outputs.version }}"
          echo "🏷️ Tag created: $TAG_NAME"

      - name: Determine hotfix version with git-cliff
        id: hotfix_version
        if: steps.check_branch.outputs.is_hotfix == 'true'
        uses: orhun/git-cliff-action@v4
        with:
          args: --bumped-version --tag-pattern "v[0-9]*"
        env:
          OUTPUT: GITHUB_OUTPUT

      - name: Create hotfix tag output
        id: hotfix_tag
        if: steps.check_branch.outputs.is_hotfix == 'true'
        run: |
          VERSION="${{ steps.hotfix_version.outputs.version }}"
          if [ -n "$VERSION" ]; then
            TAG_NAME="v${VERSION}"
            echo "tag=$TAG_NAME" >> $GITHUB_OUTPUT
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "🔥 Hotfix version determined: $VERSION"
            echo "🏷️ Hotfix tag to be created: $TAG_NAME"
          else
            echo "❌ Failed to determine hotfix version"
            exit 1
          fi

  create-pull-request:
    name: Create Pull Request
    runs-on: ubuntu-latest
    needs: version-and-tag
    if: needs.version-and-tag.outputs.version
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.REPO_PAT }}

      - name: Create Pull Request
        env:
          GH_TOKEN: ${{ secrets.REPO_PAT }}
        run: |
          CURRENT_BRANCH="${{ github.ref_name }}"
          VERSION="${{ needs.version-and-tag.outputs.version }}"
          TAG_NAME="${{ needs.version-and-tag.outputs.tag_name }}"
          IS_HOTFIX="${{ needs.version-and-tag.outputs.is_hotfix }}"

          # Determine if this is a release or hotfix
          if [[ "$IS_HOTFIX" == "true" ]]; then
            PR_TITLE="Hotfix $TAG_NAME"
            PR_BODY=$(cat << EOF
          � Hotfix $TAG_NAME

          This PR contains the hotfix branch for version $VERSION.

          ## Changes
          - Hotfix for critical issue
          - Version will be $VERSION once tagged

          ## 🚨 MANUAL TAGGING REQUIRED
          **IMPORTANT:** Before merging this PR, you must manually create the git tag:

          \`\`\`bash
          # After the final commit is added to this PR:
          git checkout $CURRENT_BRANCH
          git pull origin $CURRENT_BRANCH
          git tag -a $TAG_NAME -m "Hotfix $TAG_NAME"
          git push origin $TAG_NAME
          \`\`\`

          ## ⚠️ Important Merge Instructions
          1. **First**: Apply the git tag $TAG_NAME to the last commit in this PR
          2. **Then**: Use either "Create a merge commit" or "Rebase and merge" (DO NOT use "Squash and merge")
          3. This preserves the git tag on the correct commit for production deployment

          ## Post-Merge Actions
          After merging, the GitHub release will be created automatically using the tag.

          Urgent hotfix - ready for review and merge to main.
          EOF
          )
          else
            PR_TITLE="Release $TAG_NAME"
            PR_BODY=$(cat << EOF
          � Release $TAG_NAME

          This PR contains the release branch for version $VERSION.

          ## Changes
          - Version bumped to $VERSION
          - Changelog updated
          - Tag $TAG_NAME created

          ## Semantic Release Output
          - Version: $VERSION
          - Tag: $TAG_NAME
          - Changelog generated

          ## ⚠️ Important Merge Instructions
          **DO NOT use "Squash and merge"** - Use either "Create a merge commit" or "Rebase and merge" to preserve the git tag on the correct commit for production deployment.

          Ready for review and merge to main.
          EOF
          )
          fi

          # Check if PR already exists
          if gh pr view "$CURRENT_BRANCH" >/dev/null 2>&1; then
            echo "⚠️ PR for branch $CURRENT_BRANCH already exists"
            gh pr view "$CURRENT_BRANCH"
          else
            # Create the PR
            gh pr create \
              --title "$PR_TITLE" \
              --body "$PR_BODY" \
              --base main \
              --head "$CURRENT_BRANCH"

            echo "✅ Pull request created: $PR_TITLE"
          fi

  create-github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [version-and-tag, create-pull-request]
    if: needs.version-and-tag.outputs.version && needs.version-and-tag.outputs.is_hotfix == 'false'
    environment: Release  # 🔑 Manual approval required here!
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.REPO_PAT }}
        run: |
          TAG_NAME="${{ needs.version-and-tag.outputs.tag_name }}"

          # Check if release already exists
          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            echo "⚠️ Release $TAG_NAME already exists, skipping release creation"
            exit 0
          fi

          # Try to get changelog content for this version
          CHANGELOG_CONTENT=""
          if [ -f CHANGELOG.md ]; then
            # Extract the section for this version from changelog
            VERSION=${TAG_NAME#v}  # Remove 'v' prefix
            CHANGELOG_CONTENT=$(awk "/^## \[?${VERSION}\]?|^## ${VERSION}[^0-9]/{flag=1; next} /^## \[?[0-9]/{flag=0} flag" CHANGELOG.md | sed '/^$/d' || echo "")
          fi

          if [ -z "$CHANGELOG_CONTENT" ]; then
            # Fallback to auto-generated notes
            gh release create "$TAG_NAME" \
              --title "Release $TAG_NAME" \
              --generate-notes
          else
            # Use changelog content
            gh release create "$TAG_NAME" \
              --title "Release $TAG_NAME" \
              --notes "$CHANGELOG_CONTENT"
          fi

          echo "✅ GitHub release $TAG_NAME created successfully"

  create-hotfix-release:
    name: Create Hotfix GitHub Release
    runs-on: ubuntu-latest
    needs: [version-and-tag, create-pull-request]
    if: needs.version-and-tag.outputs.version && needs.version-and-tag.outputs.is_hotfix == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Wait for manual tag and create release
        env:
          GH_TOKEN: ${{ secrets.REPO_PAT }}
        run: |
          TAG_NAME="${{ needs.version-and-tag.outputs.tag_name }}"

          echo "🔥 Hotfix release process"
          echo "⏳ This job will wait for the manual tag $TAG_NAME to be created"
          echo "📝 Please follow the instructions in the PR to manually create the tag before merging"
          echo ""
          echo "Manual tagging command:"
          echo "  git checkout ${{ github.ref_name }}"
          echo "  git pull origin ${{ github.ref_name }}"
          echo "  git tag -a $TAG_NAME -m \"Hotfix $TAG_NAME\""
          echo "  git push origin $TAG_NAME"
          echo ""
          echo "⚠️  After the tag is created and PR is merged, create the GitHub release manually or"
          echo "    re-run this workflow to automatically create the release."

          # Check if tag exists
          if git tag -l | grep -q "^${TAG_NAME}$"; then
            echo "✅ Tag $TAG_NAME found, creating GitHub release"

            # Check if release already exists
            if gh release view "$TAG_NAME" >/dev/null 2>&1; then
              echo "⚠️ Release $TAG_NAME already exists, skipping release creation"
              exit 0
            fi

            # Create the release
            RELEASE_NOTES="🔥 Hotfix release $TAG_NAME - This is a hotfix release addressing critical issues. See the pull request for details on the changes included in this hotfix."
            gh release create "$TAG_NAME" \
              --title "Hotfix $TAG_NAME" \
              --notes "$RELEASE_NOTES" \
              --latest

            echo "✅ GitHub hotfix release $TAG_NAME created successfully"
          else
            echo "⚠️ Tag $TAG_NAME not found. Please create the tag manually as instructed in the PR."
            echo "The release will be created automatically once the tag exists."
          fi